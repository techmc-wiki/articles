---

slug: ticket-types
title: 加载票系统中的类型
title-en: "Ticket Types in the Loading System"
author: RoyanAB
co-authors:
- Arcadi4
- BFladderbeanawa
date: '2025-02-11T16:54:10+08:00'
lastmod: '2026-04-05T22:16:41+08:00'
index: 1
is-advanced: false
banner:
  src: img/01Ticket.png
  alt: img.png
---

This chapter establishes a basic understanding of the concept of loading tickets.

## Loading Ticket

A loading ticket contains:

`net.minecraft.server.world.ChunkTicket`

- `type` Loading ticket type, records the reason for loading.
- `level` Loading level, determines the chunk's computation.
- `tickCreated` Creation time, used to determine if the loading ticket has expired.

## Ticket Type

There are 8 types of loading tickets in Minecraft:

`net.minecraft.server.world.ChunkTicketType`

```java
    public static final ChunkTicketType<Unit> START = create("start", (a, b) -> 0);
    public static final ChunkTicketType<Unit> DRAGON = create("dragon", (a, b) -> 0);
    public static final ChunkTicketType<ChunkPos> PLAYER = create("player", Comparator.comparingLong(ChunkPos::toLong));
    public static final ChunkTicketType<ChunkPos> FORCED = create("forced", Comparator.comparingLong(ChunkPos::toLong));
    public static final ChunkTicketType<ChunkPos> LIGHT = create("light", Comparator.comparingLong(ChunkPos::toLong));
    public static final ChunkTicketType<BlockPos> PORTAL = create("portal", Vec3i::compareTo, 300);
    public static final ChunkTicketType<Integer> POST_TELEPORT = create("post_teleport", Integer::compareTo, 5);
    public static final ChunkTicketType<ChunkPos> UNKNOWN = create("unknown", Comparator.comparingLong(ChunkPos::toLong), 1);
```

- `start`: Used to load the spawn point when the world starts. Loading distance is 11, i.e., loading level 22. `net.minecraft.server.MinecraftServer#prepareStartRegion`
- `dragon`: Used to load the End main island during the dragon fight. Loading distance is 9, i.e., loading level 24. `net.minecraft.entity.boss.dragon.EnderDragonFight#tick`
- `player`: Player loading.
- `forced`: Used to implement the `/forceload` command and force-load spawn points.
- `light`: Used to load chunks that need light calculation.
- `portal`: Used to load chunks in the target dimension when entities or players pass through a portal. After 300gt (15s) without use, the associated loading ticket is removed. Loading distance is 3. `net.minecraft.world.PortalForcer#getPortalRect`
- `post_teleport`: Used to load target chunks when entities or players teleport, such as with `/tp`. Removed after 5gt.
- `unknown`: Used when `getChunk` is called in the game code to load chunks. Removed after 1gt.

The loading level corresponds to the loading distance, with the algorithm:

Loading level = 33 - Loading distance

`net.minecraft.server.world.ChunkTicketManager#addTicket`

```java
  ChunkTicket<T> chunkTicket = new ChunkTicket<>(type, ChunkLevels.getLevelFromType(ChunkLevelType.FULL) - radius, argument); //ChunkLevelType.FULL = 33
```

Note that the loading distance is not specified by `ChunkTicketType`. For example, `post_teleport` has a loading distance of 1 when used with the `/tp` command, but 0 in other cases.

## Loading Level

The loading level is restricted to [22-45]. As for why the upper limit is this number:

```java
public static final int INACCESSIBLE = 33 + ChunkStatus.getMaxDistanceFromFull();
```

In 1.20.1, ChunkStatus.getMaxDistanceFromFull() returns 12. See section 1.6 for details.

## Level Type

The loading level determines what this chunk can compute. There are four types:

`net.minecraft.server.world.ChunkLevelType`

```java
public enum ChunkLevelType {
    INACCESSIBLE,
    FULL,
    BLOCK_TICKING,
    ENTITY_TICKING;
}
```

The relationship between loading level and level type:

`net.minecraft.server.world.ChunkLevels#getType`

```java
    public static ChunkLevelType getType(int level) {
        if (level <= 31) {
            return ChunkLevelType.ENTITY_TICKING;
        } else if (level <= 32) {
            return ChunkLevelType.BLOCK_TICKING;
        } else {
            return level <= 33 ? ChunkLevelType.FULL : ChunkLevelType.INACCESSIBLE;
        }
    }
```

The level type determines chunk computation.

- Entity Ticking (strong loading) `ENTITY_TICKING`: When the loading level is less than or equal to 31, entities are computed. All game processes are calculated, including entities and more.
- Block Ticking (weak loading) `BLOCK_TICKING`: When the loading level is less than or equal to 32, basic computation occurs. Entities are not computed, but other things compute normally, such as redstone components.
- Loading Boundary `FULL`: When the loading level is 33, it's the loading boundary. Almost no game process computation occurs, but entities are recorded, meaning entities within the loading boundary are counted toward the mob cap.
- Inaccessible `INACCESSIBLE`: When the loading level is greater than 33, it's inaccessible. These chunks are not truly loaded, only partial world generation is performed.

## ChunkHolder

Not within the scope of this article, but a brief introduction to ChunkHolder's role.

`ChunkHolder` is the container for chunks in the underlying chunk management system. It stores the chunk itself, the chunk loading level, the chunk generation status, and the chunk's allowed uses. It can roughly be seen as the instance of a chunk within the game.

## ChunkStatus

Not within the scope of this article, but a brief introduction to ChunkStatus.

`ChunkStatus` corresponds to the chunk generation stage. In 1.20.1, there are:

- `empty`: Empty chunk;
- `structure_starts`: Obtain structures that may start generating in the chunk;
- `structure_reference`: Used for world generation;
- `biomes`: Determine biomes;
- `noise`: Generate the rough outline of terrain and bedrock layer;
- `surface`: Replace blocks near the surface;
- `carvers`: Generate caves and ravines;
- `features`: Generate underwater caves and ravines;
- `initialize_light`: Calculate the initial lighting of the chunk?;
- `light`: Calculate the lighting of the chunk;
- `full`: Chunk loading complete, `ProtoChunk` converts to `WorldChunk`.

Chunk status corresponding to loading level:

- 34-: `full`;
- 35: `initialize_light`;
- 36: `carvers`;
- 37: `biomes`;
- 38~45: `structure_starts`.
